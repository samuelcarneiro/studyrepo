/**
 * Executando o FOR em uma String
 */

const nome = 'Samuel Carneiro';

for (let i = 0; i < nome.length; i++) {
    const letra = nome[i]; //Mostrará cara letra em uma linha
    console.log(letra); 
}