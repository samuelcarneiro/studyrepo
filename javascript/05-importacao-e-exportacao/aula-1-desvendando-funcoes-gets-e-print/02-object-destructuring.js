// Importando as funções de outro arquivo (na mesma pasta)
const { gets, print } = require("./01-criando-a-funcao-gets-e-print");

print(gets()); // Retornará 10 - Valor retornado lá na função Gets


//const pessoa = {
//    nome: 'Samuel'
//}
//const { nome } = pessoa;
//const nome = pessoa.nome;
// Ambos comandos são equivalentes